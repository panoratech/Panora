version: 2
authtoken: AUTH_TOKEN # Grab your auth token here (https://dashboard.ngrok.com/get-started/your-authtoken)
log_level: debug
log: stdout

tunnels:
  api-tunnel:
    proto: http
    addr: 3000
    domain: DOMAIN # Grab your domain here (https://dashboard.ngrok.com/cloud-edge/domains)

