/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type CreateBatchLinkedUserDto = {
    linkedUserOriginIds: Array<string>;
    alias: string;
    idProject: string;
};

/** @internal */
export namespace CreateBatchLinkedUserDto$ {
    export const inboundSchema: z.ZodType<CreateBatchLinkedUserDto, z.ZodTypeDef, unknown> = z
        .object({
            linked_user_origin_ids: z.array(z.string()),
            alias: z.string(),
            id_project: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                linked_user_origin_ids: "linkedUserOriginIds",
                id_project: "idProject",
            });
        });

    export type Outbound = {
        linked_user_origin_ids: Array<string>;
        alias: string;
        id_project: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, CreateBatchLinkedUserDto> = z
        .object({
            linkedUserOriginIds: z.array(z.string()),
            alias: z.string(),
            idProject: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                linkedUserOriginIds: "linked_user_origin_ids",
                idProject: "id_project",
            });
        });
}
