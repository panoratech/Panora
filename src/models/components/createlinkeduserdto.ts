/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { remap as remap$ } from "../../lib/primitives.js";
import * as z from "zod";

export type CreateLinkedUserDto = {
    linkedUserOriginId: string;
    alias: string;
    idProject: string;
};

/** @internal */
export namespace CreateLinkedUserDto$ {
    export const inboundSchema: z.ZodType<CreateLinkedUserDto, z.ZodTypeDef, unknown> = z
        .object({
            linked_user_origin_id: z.string(),
            alias: z.string(),
            id_project: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                linked_user_origin_id: "linkedUserOriginId",
                id_project: "idProject",
            });
        });

    export type Outbound = {
        linked_user_origin_id: string;
        alias: string;
        id_project: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, CreateLinkedUserDto> = z
        .object({
            linkedUserOriginId: z.string(),
            alias: z.string(),
            idProject: z.string(),
        })
        .transform((v) => {
            return remap$(v, {
                linkedUserOriginId: "linked_user_origin_id",
                idProject: "id_project",
            });
        });
}
